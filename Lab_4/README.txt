Activity:

The query in 3 fails due to a foreign key constraint because you cannot manipulate the parent row.

The first query in 5 fails because you cannot insert duplicate primary keys.

Both queries in 8 fail due to a foreign key constraint because you cannot manipulate the child rows.